spring.application.name=fitMap

# MySQL 연결 설정
spring.datasource.url=jdbc:mysql://localhost:3306/login
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.properties.hibernate.show_sql=true
#spring.jpa.properties.hibernate.format_sql=true
#spring.jap.properties.hibernate.default_batch_fetch_size: 1000

# HikariCP (Connection Pooling) 설정 (기본값으로 설정되지만, 세부 설정도 가능합니다)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=60000

spring.thymeleaf.enable-spring-el-compiler=true
# JPA 설정 (Hibernate 사용)


# MyBatis 설정
#mybatis.mapper-locations=classpath:mybatis/ProgramMapper.xml
##mybatis.type-aliases-package=com.sports.fitMap.model

